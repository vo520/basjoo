# 前端架构文档

## 1. 技术栈

- **框架**: Next.js 14 (App Router)
- **UI 库**: React 18
- **语言**: TypeScript
- **国际化**: i18next
- **路由**: React Router v7
- **测试**: Vitest + Testing Library

## 2. 目录结构

```
frontend-nextjs/
├── app/                      # Next.js App Router
│   ├── (auth)/              # 认证路由组
│   │   ├── layout.tsx      # 认证布局
│   │   ├── login/
│   │   ├── register/
│   │   └── setup/
│   ├── (dashboard)/         # 仪表盘路由组
│   │   ├── layout.tsx      # 仪表盘布局
│   │   ├── page.tsx        # 首页
│   │   ├── agents/
│   │   ├── chat/
│   │   ├── files/
│   │   ├── knowledge/
│   │   ├── playground/
│   │   ├── sessions/
│   │   ├── urls/
│   │   ├── users/
│   │   └── settings/
│   └── layout.tsx          # 根布局
├── src/
│   ├── components/          # 共享组件
│   ├── views/              # 页面视图
│   ├── context/            # React Context
│   ├── hooks/              # 自定义 Hooks
│   ├── services/           # API 服务
│   ├── lib/               # 工具函数
│   └── locales/            # 翻译文件
└── tests/                  # 测试
```

## 3. 路由结构

### 3.1 认证路由 (无布局)

```
/login          - 登录页
/register       - 注册页
/setup          - 初始设置页
```

### 3.2 仪表盘路由

```
/               - 首页仪表盘
/agents         - Agent 列表
/agents/:id     - Agent 详情
/agents/:id/dashboard     - Agent 仪表盘
/agents/:id/knowledge     - 知识库设置
/agents/:id/settings       - Agent 设置
/agents/:id/sessions      - 会话管理
/chat/:sessionId           - 聊天页面
/files           - 文件管理
/knowledge      - 知识库
/playground     - Playground 测试
/sessions        - 会话列表
/urls           - URL 管理
/users          - 用户管理
/settings/agent - 系统设置
```

## 4. 核心组件

### 4.1 布局组件

#### AdminLayout

主布局组件，包含侧边栏和顶部导航。

```tsx
// src/components/AdminLayout.tsx
interface AdminLayoutProps {
  children: React.ReactNode;
  title?: string;
  showSidebar?: boolean;
}

export function AdminLayout({ children, title, showSidebar = true }: AdminLayoutProps) {
  return (
    <div className="admin-layout">
      {showSidebar && <Sidebar />}
      <main className="admin-main">
        {title && <PageHeader title={title} />}
        {children}
      </main>
    </div>
  );
}
```

#### ChatPanel

聊天面板组件。

```tsx
// src/components/ChatPanel.tsx
interface ChatPanelProps {
  agentId: string;
  sessionId?: string;
  onMessageSend?: (message: string) => void;
}
```

### 4.2 表单组件

#### AISettingsForm

AI 配置表单组件。

```tsx
// src/components/AISettingsForm.tsx
interface AISettingsFormProps {
  agent: Agent;
  onSave: (settings: Partial<Agent>) => Promise<void>;
}
```

### 4.3 功能组件

| 组件 | 说明 |
|------|------|
| `AdminLayout` | 主布局 |
| `ChatPanel` | 聊天面板 |
| `AISettingsForm` | AI 设置表单 |
| `AgentPanel` | Agent 面板 |
| `AgentSelector` | Agent 选择器 |
| `Dashboard` | 仪表盘 |
| `FileUploadManagement` | 文件上传管理 |
| `KBSetupWizard` | KB 设置向导 |
| `KnowledgeBaseSetup` | 知识库设置 |
| `Login` | 登录页 |
| `Playground` | Playground |
| `Register` | 注册页 |
| `Sessions` | 会话列表 |
| `URLManagement` | URL 管理 |
| `MarkdownRenderer` | Markdown 渲染 |
| `SourcesSummary` | 来源摘要 |

## 5. Context

### 5.1 AuthContext

认证上下文，管理登录状态。

```tsx
// src/context/AuthContext.tsx
interface AuthContextType {
  user: AdminUser | null;
  token: string | null;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
  isLoading: boolean;
}

export const AuthContext = createContext<AuthContextType | null>(null);
```

**存储**: localStorage

**Key**: `basjoo_auth_token`

### 5.2 ThemeContext

主题上下文。

```tsx
// src/context/ThemeContext.tsx
interface ThemeContextType {
  theme: 'light' | 'dark' | 'auto';
  setTheme: (theme: 'light' | 'dark' | 'auto') => void;
}
```

## 6. Hooks

### 6.1 useAgentKbStatus

检查 Agent 知识库状态。

```tsx
// src/hooks/useAgentKbStatus.ts
export function useAgentKbStatus(agentId: string) {
  const [status, setStatus] = useState<KbStatus | null>(null);
  const [loading, setLoading] = useState(true);
  // ...
  return { status, loading, recheck };
}
```

### 6.2 useMediaQuery

媒体查询 Hook。

```tsx
// src/hooks/useMediaQuery.ts
export function useMediaQuery(query: string): boolean {
  // ...
}
```

## 7. API 服务

### 7.1 API 客户端

```tsx
// src/services/api.ts
const API_BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:8000';

class ApiClient {
  private baseUrl: string;
  private token: string | null;

  constructor(baseUrl: string) {
    this.baseUrl = baseUrl;
    this.token = null;
  }

  setToken(token: string) {
    this.token = token;
  }

  async request<T>(config: RequestConfig): Promise<T> {
    // ...
  }
}

export const api = new ApiClient(API_BASE_URL);
```

### 7.2 API 方法

```tsx
// Chat
api.chat(message, agentId, sessionId);
api.chatStream(message, agentId, sessionId, onMessage, onError);
api.getMessages(sessionId);

// Agent
api.getAgents();
api.getAgent(agentId);
api.createAgent(data);
api.updateAgent(agentId, data);
api.deleteAgent(agentId);

// URL
api.createUrls(agentId, urls);
api.getUrls(agentId);
api.refetchUrls(agentId, urlIds);
api.deleteUrl(agentId, urlId);

// File
api.uploadFiles(agentId, files);
api.getFiles(agentId);
api.deleteFile(agentId, fileId);

// Session
api.getSessions(agentId);
api.getMessages(sessionId);
api.takeoverSession(sessionId);

// Auth
api.login(email, password);
api.register(email, password, name);
api.getMe();
```

## 8. 国际化

### 8.1 配置

```tsx
// src/i18n/config.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      'zh-CN': { translation: require('./locales/zh-CN') },
      'en-US': { translation: require('./locales/en-US') },
    },
    fallbackLng: 'en-US',
    interpolation: { escapeValue: false },
  });
```

### 8.2 翻译文件

```
src/locales/
├── zh-CN/
│   ├── auth.json
│   └── common.json
└── en-US/
    ├── auth.json
    └── common.json
```

## 9. 样式

### 9.1 样式方案

使用 Tailwind CSS 或内联样式。

### 9.2 全局样式

```css
/* src/index.css */
:root {
  --primary-color: #06B6D4;
  --widget-color: #06B6D4;
}

* {
  box-sizing: border-box;
}
```

## 10. 状态管理

### 10.1 本地状态

使用 React `useState` 管理组件状态。

### 10.2 全局状态

使用 Context:
- `AuthContext`: 认证状态
- `ThemeContext`: 主题状态

### 10.3 服务端状态

使用 SWR 或 React Query (可选)。

## 11. 路由守卫

### 11.1 RequireAuth

需要认证的路由守卫。

```tsx
// src/components/RequireAuth.tsx
export function RequireAuth({ children }: { children: React.ReactNode }) {
  const { user, isLoading } = useAuth();
  
  if (isLoading) {
    return <LoadingSpinner />;
  }
  
  if (!user) {
    return <Navigate to="/login" replace />;
  }
  
  return children;
}
```

## 12. 错误处理

### 12.1 ErrorBoundary

错误边界组件。

```tsx
// src/components/ErrorBoundary.tsx
export class ErrorBoundary extends React.Component {
  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    // 记录错误
  }
  
  render() {
    return <FallbackComponent />;
  }
}
```

### 12.2 API 错误处理

```tsx
try {
  await api.login(email, password);
} catch (error) {
  if (error.code === 'INVALID_CREDENTIALS') {
    setError('邮箱或密码错误');
  }
}
```

## 13. 测试

### 13.1 Vitest 配置

```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './tests/setup.ts',
  },
});
```

### 13.2 测试示例

```tsx
// tests/unit/AgentSettings.test.tsx
import { render, screen } from '@testing-library/react';
import { AgentSettings } from '@/views/AgentSettings';

describe('AgentSettings', () => {
  it('renders agent name', () => {
    render(<AgentSettings agentId="agt_test" />);
    expect(screen.getByText('Agent Settings')).toBeInTheDocument();
  });
});
```

## 14. 构建和部署

### 14.1 环境变量

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

### 14.2 构建

```bash
npm run build
```

### 14.3 启动

```bash
npm run start
```
